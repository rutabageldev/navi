version: "2"

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - goimports
    - gosec
    - bodyclose
    - contextcheck
    - noctx
    - exhaustive
    - godot
    - misspell

linters-settings:
  goimports:
    local-prefixes: github.com/rutabageldev/navi

issues:
  exclude-rules:
    - linters:
        - gosec
      text: "G104"  # Errors unhandled — handled by errcheck

run:
  timeout: 5m
