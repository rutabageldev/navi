# Session Handoff — 2026-04-06

**Branch:** `feat/p0-hello-world`
**Status:** Phase 3 complete. Phase 3b documented and blocked on Foundation. Phase 4 partially started (Vault token provisioned).

---

## What Was Done

### Phase 3 — services/digest Hello World (commits 1cfa47d, 6fb8c1b)

Implemented the full P0 service skeleton for `services/digest`:

- OpenAPI 3.1 spec (`services/digest/api/openapi.yaml`) with `/v1/health/live` and `/v1/health/ready`
- oapi-codegen config and generated chi-server bindings (`services/digest/internal/api/gen/api.gen.go`)
- Health handlers (`services/digest/internal/api/handlers.go`) — liveness always 200; readiness runs Vault/Postgres/NATS checks in parallel with 5s timeout, returns 503 on any failure
- `services/digest/cmd/digest/main.go` — full startup: Vault → secrets → OTEL → slog → Postgres migrations → NATS JetStream → HTTP server → graceful shutdown
- Multi-stage Dockerfile (`golang:1.25-alpine` builder, `alpine:3.20` final)
- `docker-compose.yml` (prod), `docker-compose.staging.yml`, `docker-compose.dev.yml`
- `scripts/check-generated.sh` and `scripts/validate-schemas.sh`
- Initial no-op migration baseline (`0001_init.up/down.sql`)

**Two corrections applied during Phase 3:**

1. **Foundation services, not local containers.** Dev compose runs only the digest service with `network_mode: host`, connecting to Foundation services at `10.0.40.10`. Saved to persistent memory.
2. **Vault is HTTPS.** All compose files corrected from `http://` to `https://10.0.40.10:8200`. Foundation CA cert mounted into each container.

### govulncheck pre-commit hook (commit 145d768)

- `govulncheck-on-gomod-change` hook — triggers only on `go.mod` changes
- `scripts/govulncheck.sh` wrapper — workspace-aware, reads toolchain from `go.work`
- Pinned `toolchain go1.25.8` — resolved four live stdlib CVEs on first run
- ADR-0012 updated with four-layer CVE coverage table and corrected Go hooks section

### Phase 4 partial — Vault token provisioned (end of session)

Foundation provisioned the Navi Vault token:
- **Policy:** `navi` — read/write/delete on `secret/data/navi/*`
- **Period:** 90-day periodic, renewable indefinitely
- **Location:** `/opt/navi/.env` as `VAULT_TOKEN=...` (gitignored)

Phase 4 task 4.5 (automated token renewal) added to the plan: weekly cron via `scripts/renew-vault-token.sh`, JSON logging, Promtail observability wired in Phase 5.

---

## What Is In Flight

### Phase 3b — NATS mTLS + NKEY (blocked on Foundation, code not yet written)

Discovered during Phase 4 pre-flight: the ruby-core NATS server requires NKEY signing and mTLS from all clients. The current `services/internal/nats.Connect(url string)` does neither and will be rejected by the live server.

**What needs to happen next session:**

**Code (can be done immediately):**
1. Update `services/internal/nats/nats.go` — replace `Connect(url string)` with `Connect(cfg Config)`. Add `Config` struct with `URL`, `NKeySeed`, `TLSCert`, `TLSKey`, `TLSCA`. Implement NKEY + mTLS options following ruby-core `boot.ConnectNATS()`. Add `nkeys` as a direct dep.
2. Update `services/digest/cmd/digest/main.go` — add `loadNATSConfig()` that reads from two Vault paths: `secret/data/navi/{env}/nats` (fields: `url`, `seed`) and `secret/data/navi/{env}/nats/tls` (fields: `cert`, `key`, `ca`).
3. Update ADR-0002 to document NATS auth requirements.

**Foundation (owner: you, tomorrow):**
- Generate Navi NKEY (seed + public key)
- Issue Navi client TLS cert signed by NATS CA
- Register Navi public key in ruby-core NATS server authorized users
- Seed Vault:
  - `secret/navi/{env}/nats` → `url`, `seed`
  - `secret/navi/{env}/nats/tls` → `cert`, `key`, `ca`
- NATS URLs: dev `tls://127.0.0.1:4222`, prod `tls://127.0.0.1:4223`

Phase 4.3 (live health checks) blocks on Foundation completing this. Code can be written and committed before Foundation work is done.

---

## Open Questions / Next Steps

Recommended next session order:
1. Execute Phase 3b code tasks (nats.go + main.go + ADR-0002)
2. Confirm Foundation has seeded the NATS Vault paths
3. Resume Phase 4 — vault-seed script, live startup, SIGHUP, token renewal cron

**Remaining Phase 4 tasks (after 3b):**
1. ~~4.1 — Vault policy and token~~ ✓ Done by Foundation
2. 4.2 — `make vault-seed ENV=dev/staging/prod` (Postgres, NATS placeholder, OTel, API placeholders)
3. 4.3 — Start container, verify all health checks green
4. 4.4 — SIGHUP reload verification
5. 4.5 — `scripts/renew-vault-token.sh` + `make install-cron`

Run `make setup-infra` before 4.3 to create the `navi` Docker network.

---

## Drift Observations This Session

1. **`VAULT_ADDR` was `http://` across all compose files and the P0 plan.** Corrected. CLAUDE.md and plan updated.
2. **`docker-compose.dev.yml` in the original plan spun up local infrastructure containers.** Contradicts dev node setup. Corrected and saved to persistent memory.
3. **ADR-0002 is silent on NATS authentication.** The ADR establishes NATS as the event bus but documents nothing about NKEY auth or mTLS. To be fixed in Phase 3b (task 3b.4).

---

## Branch State

All commits are local only. Branch has not been pushed to remote.

```
4ee6c2b docs(plans): add task 4.5 — automated Vault token renewal
45a0a74 docs(sessions): update handoff — Vault token provisioned by Foundation
145d768 chore(infra): add govulncheck pre-commit hook and pin toolchain to go1.25.8
6fb8c1b docs(plans): move incomplete Phase 3 exit criteria to Phase 4
1cfa47d feat(digest): implement P0 hello world service skeleton
0e6ed7b docs(plans): move Foundation connectivity check to Phase 4
334a379 feat(infra): Phase 2 services/internal package stubs
2a3fe9b docs(adr): correct OpenAPI pre-commit hook in ADR-0012; mark Phase 1 complete
12a2fc7 chore(infra): Phase 1 repo baseline — quality gates, modules, tooling
```
